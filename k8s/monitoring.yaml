---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: prometheus-analysis
  namespace: default
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: prometheus-analysis
rules:
  - apiGroups: [""]
    resources:
      - nodes
      - nodes/proxy
      - services
      - endpoints
      - pods
    verbs: ["get", "list", "watch"]
  - apiGroups:
      - extensions
    resources:
      - ingresses
    verbs: ["get", "list", "watch"]
  - nonResourceURLs: ["/metrics"]
    verbs: ["get"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: prometheus-analysis
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: prometheus-analysis
subjects:
  - kind: ServiceAccount
    name: prometheus-analysis
    namespace: default
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: prometheus-analysis-config
data:
  prometheus.yml: |
    global:
      scrape_interval: 10s

    scrape_configs:
      - job_name: "identity-api"
        kubernetes_sd_configs:
          - role: pod
            namespaces:
              names:
                - default
        relabel_configs:
          - source_labels: [__meta_kubernetes_pod_label_app]
            action: keep
            regex: identityapi
          - source_labels: [__meta_kubernetes_pod_ip]
            target_label: __address__
            replacement: $1:8080
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: prometheus-analysis
spec:
  replicas: 1
  selector:
    matchLabels:
      app: prometheus-analysis
  template:
    metadata:
      labels:
        app: prometheus-analysis
    spec:
      serviceAccountName: prometheus-analysis
      containers:
        - name: prometheus
          image: prom/prometheus:latest
          ports:
            - containerPort: 9090
          volumeMounts:
            - name: config
              mountPath: /etc/prometheus
            - name: data
              mountPath: /prometheus
          args:
            - "--config.file=/etc/prometheus/prometheus.yml"
            - "--storage.tsdb.path=/prometheus"
      volumes:
        - name: config
          configMap:
            name: prometheus-analysis-config
        - name: data
          emptyDir: {}
---
apiVersion: v1
kind: Service
metadata:
  name: prometheus-analysis-service
spec:
  selector:
    app: prometheus-analysis
  ports:
    - protocol: TCP
      port: 9090
      targetPort: 9090
  type: LoadBalancer
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: grafana-analysis
spec:
  replicas: 1
  selector:
    matchLabels:
      app: grafana-analysis
  template:
    metadata:
      labels:
        app: grafana-analysis
    spec:
      containers:
        - name: grafana
          image: grafana/grafana:latest
          ports:
            - containerPort: 3000
          env:
            - name: GF_SECURITY_ADMIN_PASSWORD
              value: "admin"
          volumeMounts:
            - name: data
              mountPath: /var/lib/grafana
      volumes:
        - name: data
          emptyDir: {}
---
apiVersion: v1
kind: Service
metadata:
  name: grafana-analysis-service
spec:
  selector:
    app: grafana-analysis
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
  type: LoadBalancer
